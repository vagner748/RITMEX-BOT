# General Trading Settings
TRADE_SYMBOL=BTCUSDT
LOSS_LIMIT=0.03 # 3% loss limit

# Aster Exchange API Credentials
ASTER_API_KEY=
ASTER_API_SECRET=

# --- Grid Trading Configuration ---
GRID_ENABLED=true
GRID_NUM_ORDERS=10          # Total de ordens (5 buy + 5 sell)
GRID_SPACING_PCT=0.3        # Espaçamento entre ordens (%)
GRID_PROFIT_PCT=0.4         # Profit target por grid (%)
GRID_STOP_LOSS=1.0          # Stop loss por posição (%)
GRID_MAX_POSITIONS=3        # Máximo de posições simultâneas
GRID_REBALANCE_INTERVAL=300000  # Intervalo de rebalanceamento (ms)
GRID_RSI_PERIOD=14          # Período do RSI
GRID_RSI_TIMEFRAME=15m      # Timeframe do RSI (e.g., 1m, 5m, 15m)
GRID_CENTER_CANDLES=100     # Candles para calcular centro do grid
GRID_ORDER_SIZE=0.005       # Tamanho de cada ordem do grid
GRID_MIN_SPREAD=0.1         # Spread mínimo para considerar entrada (%)
